
<p>
  <strong>User:</strong>
  <%= @story.user.name %>
</p>

<p>
  <strong>Body:</strong>
  <%= @story.body %>
</p>

<% if @story.user.id == current_user.id%>
<%= link_to 'Edit', edit_story_path(@story) %> 
<% end %>
|
<%= link_to 'Back', stories_path %>

